---
phase: 04-leads-store-sheets
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/STATE.md
autonomous: false
requirements:
  - STOR-01
  - STOR-03
  - STOR-04
  - STOR-06

must_haves:
  truths:
    - "Workflow is imported and active in n8n"
    - "Google Sheets and Google Drive credentials are assigned"
    - "Sheet creation works on first run"
    - "Dedup prevents duplicates on second run"
    - "Static data persistence works when called as a sub-workflow via Execute Workflow node"
  artifacts: []
  key_links: []
---

<objective>
Import leads_store_sheets workflow into n8n, assign Google Sheets and Google Drive OAuth2 credentials, update the Share Sheet node email address, and verify the workflow functions correctly.

Purpose: The workflow JSON is a template — it needs credentials assigned and the sharing email configured in the actual n8n instance before it can run.

Output: Working workflow in n8n with assigned credentials, verified via test execution
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-leads-store-sheets/04-CONTEXT.md
@.planning/phases/04-leads-store-sheets/04-01-SUMMARY.md
@workflows/leads_store_sheets.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Import workflow and assign credentials in n8n</name>
  <files>workflows/leads_store_sheets.json</files>
  <read_first>
    workflows/leads_store_sheets.json
    .planning/phases/04-leads-store-sheets/04-CONTEXT.md
  </read_first>
  <action>
Human imports the workflow into n8n and assigns credentials. Claude cannot perform this step — it requires the n8n UI for OAuth2 credential assignment and manual email address configuration.

Steps for user:
1. In n8n UI, go to Workflows, Import from File, select workflows/leads_store_sheets.json
2. Assign Google Sheets OAuth2 credential to: Create Spreadsheet, Write Header Row, Read Existing IDs, Append Rows, and Validate Sheet Access (HTTP Request node)
3. Assign Google Drive OAuth2 credential to: Share Sheet node
4. In Share Sheet node, replace PLACEHOLDER_CREDENTIAL_EMAIL with the actual email of the Google Sheets credential account
5. Save the workflow and note the workflow ID from the URL bar
6. Test with sample input: { "posts": [{"postId": "test-001", "authorName": "Test User", "postText": "Test post", "postLink": "https://facebook.com/test", "postDate": "2026-03-22"}], "group_url": "https://www.facebook.com/groups/test-group/" }
7. Verify: Google Sheet "Facebook Leads" created with tab "test-group", header row in A1:E1, and one data row
8. Test dedup: Run again with same input — verify no new rows appended
9. NOTE: Static data only persists in production executions, not test mode. Sheet ID will not carry between "Test workflow" button runs.
10. IMPORTANT — Sub-workflow static data persistence test (RESEARCH.md Open Question 2): To validate that static data persists when this workflow is called as a sub-workflow (its real production context), create a simple test parent workflow with an Execute Workflow node that calls leads_store_sheets. Activate the parent workflow and trigger it twice with the same sample input. On the second call, verify that the Sheet ID is reused (no second "Facebook Leads" sheet created). Do NOT rely on the "Test workflow" button for this — use a real trigger (e.g., Manual Trigger in the parent, then activate and trigger via API or n8n UI "Execute" button on the active workflow).
  </action>
  <what-built>leads_store_sheets workflow JSON with all nodes for sheet creation, tab management, dedup, and append.</what-built>
  <how-to-verify>
    1. In n8n UI, go to Workflows -> Import from File -> select workflows/leads_store_sheets.json
    2. Verify all nodes appear connected in the canvas
    3. Assign Google Sheets OAuth2 credential to: Create Spreadsheet, Write Header Row, Read Existing IDs, Append Rows, and Validate Sheet Access HTTP Request node
    4. Assign Google Drive OAuth2 credential to: Share Sheet node
    5. In Share Sheet node, replace PLACEHOLDER_CREDENTIAL_EMAIL with credential account email
    6. Save the workflow and note the workflow ID
    7. Test with sample input: { "posts": [{"postId": "test-001", "authorName": "Test User", "postText": "Test post", "postLink": "https://facebook.com/test", "postDate": "2026-03-22"}], "group_url": "https://www.facebook.com/groups/test-group/" }
    8. Verify: Google Sheet "Facebook Leads" created with tab "test-group" + header row + one data row
    9. Test dedup: Run again with same input — verify zero new rows
    10. Test sub-workflow static data persistence: Create a test parent workflow with Execute Workflow node calling leads_store_sheets. Activate the parent, trigger it twice with the same input. Verify second call reuses the sheet (no duplicate "Facebook Leads" sheet). This validates RESEARCH.md Open Question 2.
  </how-to-verify>
  <verify>
    <automated>node -e "const w=require('./workflows/leads_store_sheets.json'); if(w.name==='leads_store_sheets'){console.log('Workflow JSON valid');process.exit(0)}else{process.exit(1)}"</automated>
  </verify>
  <resume-signal>Provide the n8n workflow ID and type "approved", or describe any issues encountered. Also confirm whether static data persisted in the sub-workflow test (step 10).</resume-signal>
  <acceptance_criteria>
    - Workflow imported into n8n without errors
    - All Google Sheets nodes have credential assigned
    - Google Drive Share node has credential and real email address (not PLACEHOLDER_CREDENTIAL_EMAIL)
    - Test execution creates sheet + tab + row on first run
    - Dedup verified: second run with same posts appends zero new rows
    - Sub-workflow static data persistence confirmed: calling leads_store_sheets via Execute Workflow node in a test parent workflow, second call reuses the sheet ID (no duplicate sheet created)
    - n8n workflow ID noted for STATE.md update
  </acceptance_criteria>
  <done>Workflow imported into n8n with credentials assigned and verified via test execution. Sub-workflow static data persistence confirmed. User has provided workflow ID.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md with workflow ID</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md
  </read_first>
  <action>
Update `.planning/STATE.md`:
1. In the `## n8n Workflow IDs` table, replace the leads_store_sheets row:
   - Change `| leads_store_sheets | — | Not yet created |` to `| leads_store_sheets | {WORKFLOW_ID_FROM_CHECKPOINT} | Complete |`
2. Update `## Current Status` section:
   - Add entry: `- Phase 4 (leads_store_sheets): **Complete** — Workflow imported and tested in n8n (id: {ID})`
3. Update `## Next Action` to: `Phase 5: leads_main — run /gsd:plan-phase 5`
4. Update `Current focus` to: `Phase 05 — leads-main`
  </action>
  <verify>
    <automated>grep "Complete" .planning/STATE.md && grep "Phase 5" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md n8n Workflow IDs table has leads_store_sheets row with a real workflow ID (not the dash placeholder)
    - STATE.md Current Status section contains "Phase 4 (leads_store_sheets): **Complete**"
    - STATE.md Next Action references Phase 5
  </acceptance_criteria>
  <done>STATE.md reflects Phase 4 completion with n8n workflow ID recorded, Next Action points to Phase 5</done>
</task>

</tasks>

<verification>
- Workflow runs in n8n without errors
- Google Sheet "Facebook Leads" created with correct tab and header row
- Dedup prevents duplicate rows on re-run
- Static data persists when called as sub-workflow via Execute Workflow node
- STATE.md updated with workflow ID and Phase 5 as next action
</verification>

<success_criteria>
- leads_store_sheets workflow is live in n8n with credentials assigned
- Test execution proves sheet creation, tab creation, header row, and row append
- Sub-workflow static data persistence validated
- STATE.md reflects Phase 4 completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-leads-store-sheets/04-02-SUMMARY.md`
</output>
